{
  "name": "@ln-maf/postgresql",
  "publishConfig": {
    "access": "public"
  },
  "version": "4.1.7",
  "description": "PostgreSQL support / step definitions for MAF",
  "main": "index.js",
  "scripts": {
    "test": "npm run postgres:start && npm run test:run && npm run postgres:stop",
    "test:coverage": "npm run postgres:start && npm run test:run:coverage && npm run postgres:stop",
    "test:run": "wait-on tcp:localhost:5433 && sleep 5 && mkdir -p ../../test/report && npm run cucumber:exec && npm run test:finalize",
    "test:run:coverage": "wait-on tcp:localhost:5433 && sleep 5 && mkdir -p ../../test/report && npm run cucumber:exec:coverage && npm run test:finalize",
    "cucumber:exec": "USE_ENV_VARIABLES=TRUE POSTGRESQL_SQL_USERNAME=postgres POSTGRESQL_SQL_PASSWORD=testpassword npx cucumber-js -f json:../../test/report/postgresql.json --require \"stepDefinitions/*.js\" features/$FEATURE_FILE",
    "cucumber:exec:coverage": "USE_ENV_VARIABLES=TRUE POSTGRESQL_SQL_USERNAME=postgres POSTGRESQL_SQL_PASSWORD=testpassword npx nyc --reporter=lcov --reporter=text --report-dir=../../coverage npx cucumber-js -f json:../../test/report/postgresql.json --require \"stepDefinitions/*.js\" features/$FEATURE_FILE",
    "test:finalize": "echo 'PostgreSQL tests completed'",
    "postgres:start": "docker-compose up -d && echo 'Waiting for PostgreSQL to be ready...' && sleep 10",
    "postgres:stop": "docker-compose down",
    "postgres:logs": "docker-compose logs postgres-test",
    "postgres:shell": "docker exec -it postgres-test psql -U postgres -d testdb"
  },
  "bin": {
    "postgresql-configure": "./config.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@ln-maf/default-sql": "^4.1.7",
    "keytar": "^7.9.0",
    "pg": "^8.0.0"
  },
  "peerDependencies": {
    "@cucumber/cucumber": ">= 12.0.0",
    "@ln-maf/core": ">= 4.0.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.0.0",
    "@ln-maf/core": "^4.1.5",
    "@ln-maf/validations": "^4.1.5",
    "concurrently": "^8.2.2",
    "multiple-cucumber-html-reporter": "3.7.0",
    "nyc": "^17.0.0",
    "wait-on": "^7.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hpcc-systems/MAF.git"
  },
  "keywords": [
    "cucumber-js",
    "testing",
    "gherkin",
    "cucumber-steps"
  ],
  "bugs": {
    "url": "https://github.com/hpcc-systems/MAF/issues"
  },
  "gitHead": "1b3bf8c441e8de94216e2f3c8638a172a0697148"
}
